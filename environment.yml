name: arcpy
channels:
  - defaults
  - conda-forge
dependencies:
  - python = 3.9
  - numpy < 2
  - scipy
  - pinocchio
  - pip
  - pip:
    - inputs == 0.5
    - mujoco == 3.3.4
    - lcm == 1.5.1
    - casadi == 3.7.0
